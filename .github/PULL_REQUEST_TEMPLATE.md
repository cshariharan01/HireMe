## What & why
<!-- Short description of the change and the problem it solves. Link any issue. -->

## Checklist
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] Focused change (one topic)
- [ ] No secrets/data committed (`.env.local`, `data/`, `*.pdf` stay ignored)
- [ ] Updated docs (README / in-app guide / `.env.example`) if behavior changed
- [ ] If I edited the schema/ontology, I kept `src/lib/*` and the `scripts/*` copies in sync
